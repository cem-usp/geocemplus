import * as ss from 'simple-statistics'

//function to quantilize
function getQuantiles(arr, n) {
    let quantiles = ss.quantile(arr, p)

    return quantiles
}

//function to attribute to a pallet

//function to put in a layer